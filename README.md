# Jarvis project backend
## Dependencies
Execute the next pip commands to install the project dependencies:
* pip install face-recognition
* pip install -r requirements.txt (OJO ACA)
* https://cmake.org/download/
